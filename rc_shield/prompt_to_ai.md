根据合规审查与风险评级系统产品需求文档 (PRD)制作一个可交互的原型，并提供部署到 GitHub Pages 的简单说明，合规知识库内根据<香港地区规则清单>进行制作，后续会以香港地区进行演示。

其他要求如下：

1. 技术实现约束
纯静态实现：仅使用 HTML、CSS、JavaScript（无需后端代码）。
数据存储：用 localStorage 模拟数据传递（如角色信息、任务状态、审查结论）。
页面跳转：通过链接实现不同模块 / 角色页面的切换。
交互模拟：表单验证、状态变更、计算逻辑均用 JavaScript 实现，无需真实数据库和后端接口。
兼容性：支持主流浏览器（Chrome、Edge），适配桌面端即可。

2. 部署要求
最终输出物为完整的静态文件包（含 HTML、CSS、JS等），文件结构清晰。
用户上传文件至 GitHub 仓库后，通过仓库设置开启 Pages 功能即可访问

3. 交付物
完整的 HTML/CSS/JavaScript 文件包（所有页面、样式、脚本）；
部署说明（含 GitHub Pages 上传步骤）；
原型操作指南（说明各角色功能入口和流程步骤）。

4. UI 设计要求（请在此处补充你的设计需求）
主色调：蓝白灰色系，体现专业合规
布局风格：左侧导航 + 右侧内容区，简洁高效
视觉元素：使用卡片式设计（带圆角和轻微阴影）、数据表格展示列表信息、清晰的按钮交互（hover状态变化），用蓝/绿/红/黄等功能色区分状态；  
组件偏好：任务用卡片式展示，状态用色块区分
交互细节：提交时显示确认弹窗，实时计算结果用醒目标签展示
技术偏好：基于Tailwind CSS实现，支持响应式布局，确保界面简洁易操作，突出功能性和信息可读性。  

请根据以上需求生成可交互的原型，确保角色流程完整（如 Analyst 提交后，Manager 可看到待审核任务并操作），并提供部署到 GitHub Pages 的简单说明。